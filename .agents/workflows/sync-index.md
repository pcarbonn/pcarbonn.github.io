---
description: Synchronize index.html to all language folders
---

This workflow copies the root `index.html` to every language folder to ensure they all use the same template.

// turbo
1. Sync index.html using npm:
```bash
npm run sync-index
```
